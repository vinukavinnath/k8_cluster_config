apiVersion: apps/v1
kind: Deployment
metadata:
  name: elasticsearch
spec:
  replicas: 1
  selector:
    matchLabels:
      deployment: timelogger
  template:
    metadata:
      labels:
        deployment: timelogger
    spec:
      containers:
        - name: timelogger_c
          image: vinukavinnath/spring-logger-t:v1
